services:
  web:
    build: ./app
    image: academia-nexus-web
    container_name: academia-nexus-web
    ports:
      - "8080:80"
    volumes:
      - ./:/var/www/html/practica1
    depends_on:
      - db

  db:
    image: mysql:8.0
    container_name: academia-nexus-db
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: academia_nexus
      MYSQL_USER: appuser
      MYSQL_PASSWORD: apppass
    volumes:
      - db_data:/var/lib/mysql
    ports:
      - "3307:3306"

  phpmyadmin:
    image: phpmyadmin/phpmyadmin
    container_name: academia-nexus-phpmyadmin
    depends_on:
      - db
    environment:
      PMA_HOST: db
      PMA_USER: appuser
      PMA_PASSWORD: apppass
    ports:
      - "8082:80"

volumes:
  db_data:
